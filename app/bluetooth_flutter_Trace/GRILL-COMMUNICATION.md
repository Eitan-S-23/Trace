# 码表 UI 重做沟通记录（第二轮 · 深度对齐）

> 本文件实时记录 grill（拷问式对齐）过程。ACT 1：我逐项追问、给出推荐答案并锁定需求；ACT 2：把锁定的 PLAN.md 交给 Codex 对抗式评审。

## 一、代码状态确认（已核实）

### 导航链路
- 应用入口 `main.dart` → `home: MainAppPage`。
- `MainAppPage` 底部只有三个标签：设备 / 商城 / 我的。
- 码表 `SpeedometerPage` 从「设备」标签页里 `Get.to(() => const SpeedometerPage())` 打开（`device_tab_page.dart:225`）。
- `cycling_dashboard_page.dart` 是白底独立页面，未被任何导航引用，属无用代码。

### 码表现状（`lib/pages/speedometer_page.dart`，约 3874 行）
- 底部导航**已经**是目标形态：仪表盘 / 统计 / 开始-暂停（中间橙色圆钮）/ 路线 / 设备。说明上一轮已改对导航。
- 已有四个子页面：
  - 仪表盘 `_DashboardPage`：英雄卡（路线图+总距离+4 项指标）、6 个指标卡、速度&海拔图、功率/心率两个环形图。
  - 统计 `_StatisticsPage`：周/月/年/全部分段（默认「月」）、月份选择器、总览、里程趋势柱状图、运动时长趋势柱状图、运动类型分布环形图。
  - 路线 `_RoutesPage`：我的/收藏/导入分段、搜索栏、4 张路线卡片。
  - 设备 `_DevicesPage`：扫描雷达、可用设备列表、已连接设备面板（三段拼在同一页）。
- **根因问题**：整页用一个 `ListView`（`speedometer_page.dart:50`）包住所有内容，所以必须上下滚动；仪表盘内容高度 ≈900px+，远超一屏。
- **交互问题（已定位若干）**：英雄卡 `_ActivityHeroCard` 用 `Stack`，最后的 `Column` 盖住右侧 `Positioned` 的全屏/分享圆钮 → 点不到；大量按钮仅 `_showUiMessage` 弹提示。
- 数据：`_RideSample` / `_RideStats` 在有真实骑行/历史时用真实数据，否则回退到与设计图一致的示例常量（102.63km 等）。

### now.jpg 与代码的矛盾（需确认）
- now.jpg 底部导航是旧「仪表盘/分析/继续/记录/我的」+ 简单速度表盘；而仓库已提交代码是新导航 + 密集仪表盘。
- 说明手机上看到的可能不是最新提交的构建（旧构建，或新代码没成功出 Release）。直接影响你能否看到我的修改，放入本轮提问。

### 构建/发布闭环
- `.github/workflows/build.yml`：push 到 `main`/`master` 即触发 Android APK + Windows EXE 构建并自动创建 GitHub Release（tag `build-<run#>-<sha>`）。
- 当前分支 `main`。无法本地编译 Flutter，本机 `gh` 未认证；验证只能靠 push 触发 Actions 后看 Release。

## 二、设计图理解（逐张）
- **仪表盘.png**：853×1844，**单屏**。顶部设备状态条 + 标题；英雄卡（地图+102.63km+运动时间/平均速度/累计爬升/训练负荷）；6 个指标卡（平均速度/平均功率/平均心率/平均踏频/累计爬升/平均温度，每卡带迷你折线）；速度&海拔双线图；功率分布/心率分布两个环形图。
- **统计**：2048×2048 = 两张并排。
  - 月：总览(6) + 里程趋势(橙柱 05/01–05/29) + 运动时长趋势(蓝柱)。
  - 年：运动类型分布(环) + 月度统计(柱 1–12 月) + 强度分布(环, 按时间)。
  - 周：总览(6) + 里程趋势(周一–周日) + 运动时长趋势。
  - 全部：总览(6) + 运动类型分布(环) + 月度里程趋势(柱)。
  - 顶部日期条随周期变化：周=日期区间，月=2024年5月，年=2024年，全部=区间。
- **路线**：左=列表（我的/收藏/导入 + 搜索 + 路线(18) + 卡片：缩略地图/标题/日期/公路·难度/距离·爬升·时长/收藏·分享·更多）；右=路线详情（大地图、标题、距离·爬升、简介、海拔图、起终点信息、底部「发送到设备」+「导航」）。
- **设备**：左=扫描雷达 + 停止扫描 + 可用设备列表(连接) + 未找到我的设备；右=设备详情（设备图+已连接/固件/电量、设备设置/页面配置/传感器管理/自动暂停/自动计圈、设备信息、解除绑定）。
- 注：设计图自带的顶部「活动/统计/训练/路线/设备」与底部「首页/日历/记录/发现/我的」都按要求改为统一底栏（仪表盘/统计/开始暂停/路线/设备）；训练页不做。

## 三、本轮待确认问题（ACT 1 进行中）
> 为兼顾你「不必要不打断、连续执行」的偏好，本轮先批量确认 4 个互相独立的基础决策，再按答案继续追问。

- **Q1 当前运行版本** —— ✅ 已答 = ②「新版但很糟（会滚动、按钮点不动）」。
  - 结论：**构建/发布闭环是通的**，你看到的就是已提交代码；我改完 push 出 Release 即可被你看到。
- **Q2 地图方案** —— ✅ 已答 = 风格化绘制（CustomPaint 深色地图 + 有 GPS 轨迹时画真实轨迹，无密钥/无网络，构建稳定）。
- **Q4 数据来源** —— ✅ 已答 = 真实优先 + 示例兜底（有真实数据用真实值，无数据用与设计图一致的示例值）。
- **Q3 单屏 vs 滚动** —— ✅ 已答（关键）：采用 `功率计`（`power_meter_page.dart`）那种**「固定头部 + 有界滚动区」**模式。
  - 既有范式：`Column( [固定状态卡, 固定扫描控制, Expanded( ListView 设备列表 )] )` —— 头部/控件钉死不动且永远可点，只有指定内容区在内部滚动。
  - 现状错误：码表把所有东西（含顶部栏、tab）塞进**单一 ListView**，导致整页滚动 + 按钮被挤到点不到。
  - 套用到四页：
    - 设备：顶部栏 + 扫描雷达 + 扫描控制**固定**；可用设备**列表在 Expanded 内滚动**（正是你举的例子）。
    - 路线：顶部栏 + 我的/收藏/导入 + 搜索 + 计数**固定**；路线列表在 Expanded 内滚动。
    - 统计：顶部栏 + 周/月/年/全部 + 日期选择器**固定**；面板区按设计图紧凑排，优先一屏放下，放不下时在 Expanded 内滚动。
    - 仪表盘：顶部栏**固定**；按设计图紧凑比例优先一屏放下；超短屏时内容区在 Expanded 内滚动，但不再滚动顶部栏。
  - 不再使用「整页单一 ListView」。

## 四、补充决策（ACT 1 续 · 待你确认）
- **D1 详情页导航**：路线卡片/设备项点击 → `Get.to()` push 到详情页（带 ‹ 返回），与设计图右半屏一致。本轮**包含**路线详情、设备详情两个详情页。推荐：是。
- **D2 外围按钮**：核心交互（切页、周期切换、开始/暂停、路线→详情、设备→详情、扫描开关、收藏/搜索框）做成真实可用；非核心按钮（分享、全屏、更多、固件检查更新、页面配置等）先做成优雅占位（toast/弹窗提示），不做后端。推荐：是。
- **D3 骑行实时显示**：仪表盘始终用设计图的概览版式；按「开始」记录时，英雄卡里的距离/时间/速度/爬升等**就地变为实时值**（已有 `_RideSample` 接 `RideController`），**不**新增 now.jpg 那种独立速度表盘页（now.jpg 是旧版，非目标）。推荐：是。
- **D4 训练页**：不做（你的底栏无训练）。
